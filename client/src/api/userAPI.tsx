const retrieveUsers = async () => {
  try {
    // TODO: Add a fetch to request the user data from the server
    // * Hint the api endpoint is at '/api/users'

  } catch (err) {
    console.log('Error from data retrieval:', err);
    return [];
  }
};

export { retrieveUsers };
